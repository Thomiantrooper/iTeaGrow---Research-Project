{
  "name": "iteagrow-web-application",
  "version": "1.0.0",
  "description": "iTeaGrow is a modern web application designed for the Tea Industry, built using the MERN stack (MongoDB, Express.js, React, Node.js).",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"node scripts/keep_alive.js\"",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "backend": "npm run server",
    "client": "cd frontend && npm run dev",
    "frontend": "npm run client",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ..",
    "build": "cd frontend && npm run build",
    "push-all": "git push origin main && git push research main:iTeaGrow-Web-Application",
    "keep-alive": "node scripts/keep_alive.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}